# Normalize line endings: LF in the repo, on every platform.
* text=auto eol=lf

# Binary assets are left untouched.
*.png binary
*.ico binary
*.icns binary

# cargo-about preserves upstream license text, including upstream trailing spaces.
THIRD_PARTY_LICENSES.txt text eol=lf -whitespace
